---
  - name: "make sure static directory exists"
    file:
      path: "{{ staticDir }}/"
      state: directory

  - name: "make sure static HTML subdirectory exists"
    file:
      path: "{{ staticDir }}/html/"
      state: directory

  - name: "create region files"
    template:
      src: "region.html"
      dest: "{{ staticDir }}/html/{{ item.shortname }}.html"
    with_items: "{{ regions }}"
